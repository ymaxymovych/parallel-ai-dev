# Скрипти комплекту мусять приїжджати з LF навіть на Windows.
# Перевірено 10.08.2026: git-bash витримує CRLF і зараз, тож це не аварія —
# це прибирання цілого класу «у мене не запускається», який інакше чекає
# першого студента з іншою збіркою bash.
*.sh text eol=lf
