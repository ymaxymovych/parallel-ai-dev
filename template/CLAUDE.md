# Конституція проєкту <НАЗВА>

@CLAUDE.parallel-ai-dev.md

> Рядок вище під'єднує правила кіту parallel-ai-dev — Claude Code вклеює той
> файл у контекст кожного чату разом із цим. НЕ редагуй `CLAUDE.parallel-ai-dev.md`
> (він оновлюється кітом); свої правила пиши ТУТ — цей файл кіт ніколи не чіпає.

## Зони цього проєкту (заповни)

- Ризикові зони (гейт координатора): `<перелічи шляхи: auth, платежі, схема БД…>`
- Команда перевірки перед PR: `<напр. npm test && npm run lint && npm run build>`
- Команда деплою: `<напр. bash deploy.sh>` — тільки координатор.
